# Last30 child method

Load this reference when `research --last30` or recent-community research selects
last30. Use the child engine at `references/last30days/`, not a plugin cache,
marketplace checkout, or the historical bundled backend.

1. Read `last30days/METHOD.md` for mode selection, query planning, source selection,
   comparison, doctor, synthesis, and saved-brief procedures. The explicit canonical
   child path supersedes its cache-selection Step 0; preserve all other applicable
   source contracts. Parent instructions determine output citation formatting.
2. Inspect the current engine's `--help` using Python 3.12+. Do not install anything
   during research. Missing runtime is an explicit prerequisite.
3. Use `SKILL_DIR=/Users/jean-patricksmith/.agents/skills/research/references/last30days`.
   Invoke `$SKILL_DIR/scripts/last30days.py` with the chosen existing engine mode.
   Existing global configuration, saved-state roots and credentials stay in place.
4. For login-backed sources, follow the child's cached doctor preflight and show
   source availability. Do not collect or display cookie/token values.
5. Execute within the agreed provider/time/query budget. Record requested versus
   observed sources, timestamps and partial-provider failures. Unknown dates cannot
   establish 30-day freshness. No posts from a provider does not establish no activity.
6. Return source-bearing findings and limitations. Existing comparison, briefing,
   saved HTML, watchlist, discovery and library modes remain conditional capabilities;
   invoke only the requested mode. Publication, scheduled monitoring and credential
   setup require their existing authorization.

`research --last30` is prompt syntax; runtime uses the child Python CLI or the
existing Lev workflow adapter. Neither a native `lev research --last30` flag nor
universal provider health is claimed.
